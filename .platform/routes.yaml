http://www.{default}/:
  to: https://{default}/
  type: redirect
http://{default}/:
  cache:
    enabled: true
  ssi:
    enabled: false
  type: upstream
  upstream: php:php
https://www.{default}/:
  to: https://{default}/
  type: redirect
https://{default}/:
  cache:
    cookies:
    - '*'
    default_ttl: 0
    enabled: true
    headers:
    - Accept
    - Accept-Language
  ssi:
    enabled: false
  type: upstream
  upstream: php:php
